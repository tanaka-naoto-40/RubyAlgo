<tr>
  <td>
    <%= lesson.id %>
  </td>
  <td>
    <%= lesson.title %>
  </td>
  <td>
    <%= lesson.content %>
  </td>
  <td>
    <%= lesson.tips %>
  </td>
  <%
=begin%>
 <td>
    <%= link_to t('defaults.show'), admin_lesson_path(lesson), class: 'btn btn-info' %>
    <%= link_to t('defaults.edit'), edit_admin_lesson_path(lesson), class: 'btn btn-success' %>
    <%= link_to t('defaults.delete'), admin_lesson_path(lesson), method: :delete, data: { confirm: t('defaults.message.delete_confirm') }, class: 'btn btn-danger' %>
  </td> 
<%
=end%>
</tr>
